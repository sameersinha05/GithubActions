name: GitHub Actions Demo
on: 
  push:
    branches: [main]
    paths-ignore: ['.github']
  pull_request: 
    branches: [main]
  schedule:
    - cron: '15 6 * * 0'
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "🎉 The job was triggered by a ${{ github['event_name'] }} event."
          echo "🎉 The name of brach is  ${{ github.ref }} and repository is ."
          
      - uses: actions/checkout@v3.3.0

      - name: List files in teh repository
        run: |
          echo " The respository  ${{ github.repository }} contains the following files:"
          tree
